line-length = 100
target-version = "py312"

exclude = [".venv", "notebooks", "data", "models"]

[lint]
select = ["E", "F", "I", "W", "B", "UP"]

[lint.per-file-ignores]
"backend/app/main.py" = ["B008"]
